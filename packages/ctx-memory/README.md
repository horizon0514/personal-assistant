# @pa/ctx-memory

Personal Memory —— 偏好/事实,可见可编辑,纯本地(核心域)

已实现:`remember`/`update_memory`/`forget_memory`/`search_memory` 四工具 + 情景/修订/受控遗忘;每 workspace 一份(本地 JSON),可见可恢复。职责与模型见 [`research/domain-model.md`](../../research/domain-model.md)。
